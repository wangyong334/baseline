# 创新点清单（2026-09-18 ~ 09-22 头脑风暴与实现的全部条目）

按信息流分层。每条给出：**改哪里 / 解决什么缺陷 / 依赖关系 / 状态**。
状态取值：`已实现` = v2-1 代码里有；`设计` = 有方案未写代码；`候选` = 只提过思路；`证伪` = 被数据或文献否定。
依赖关系里的"独立"指可以单独加到 V1 或 V2 的任意位置，不需要别的新模块配合。

---

## 实现度审计（2026-09-22）

| 口径 | 覆盖 | 说明 |
|---|---|---|
| 全部 63 条 | 17 条已实现 + 3 条部分 ≈ **27%** | 清单本来就是发散的全集，不以此为目标 |
| 方案一（统计 / 判决核心） | **≈ 85%** | 缺：轨迹级层级累加、输出为稀疏事件流、分布无关（conformal）校准、弱监督 EM、若干命题的专门实验 |
| 方案二（对称 / 等变） | **≈ 15%** | 只落地了偶极与一阶时间矩这两个零参数特征；正则坐标、等变提升、保标签增强、自监督都没做 |
| 方案三（稀疏计算） | **0%** | 分块稀疏、亚临界正则、时间金字塔、去空间下采样一项都没做 |

**结论**：论文的三个"亮点"（推导出来的累加神经元、极性偶极、负证据/泄漏）已经全部实现；缺口集中在
**支撑证据层**（真实数据上的背景条件校验、廉价基线、实测能耗）与**方案二、三**。

### 下一步优先级

| 档 | 事项 | 挂钩的主张 | 成本 |
|---|---|---|---|
| 必做 | 纯背景片段上实测告警率；阈值在 val 上确定后冻结 | 虚警上界在真实数据上是否有意义 | 低（工具已有） |
| 必做 | 分场景报告 + 廉价基线（几何特征+树、BAF/EBF 去噪器） | 网络与判决层的必要性 | 低 |
| 必做 | 把前端与判决层的逐元素运算计入能耗口径 | 能否谈能耗 | 低 |
| 必做 | 神经元饱和的处理（膜电位裁剪 / ALIF） | 脉冲稀疏性与能耗叙事 | 中 |
| 应做 | 二阶空间矩（线状度 / 面状度）进前端 | "流形维度"理论与实现的缺口 | 中 |
| 应做 | 去空间下采样 + 时间金字塔 | 6×5 像素目标在深层消失 | 中 |
| 应做 | 保标签的伽利略 / 旋转增强 + 速度外推实验 | 泛化与相机运动 | 低 |
| 应做 | 轨迹级层级累加（连通分量） | 足迹限制与长时程证据 | 中 |
| 可选 | 等变提升、速度空间主干、目标槽、弱监督 EM、稀疏执行、传感器闭环 | 下一篇 | 高 |

---

## S0 传感器层

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 1 | 恒虚警偏置闭环（用检测器的虚警估计反控相机阈值/不应期） | 传感器参数 | 噪声密度跨场景差 100 倍，相机参数却固定 | 独立（需硬件或仿真） | 候选 |

---

## S1 输入与事件表示

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 2 | **背景率模型 + 局部归一化（CFAR 式）** | 前端 | 全局 q99 截断在稠密序列饱和（022/023 占 test 59% 事件，V1 IoU 仅 0.31–0.53） | 独立可用；**同时是判决层保证的前提**（提供 μ0） | 已实现 |
| 3 | **精确时间的多尺度时间矩**（各尺度的 ratio / 平均年龄） | 前端 | 时间被量化到 10/50 ms；计数是零阶量，编码不了排列 | 独立 | 已实现 |
| 4 | **极性偶极**（ON 质心 − OFF 质心） | 前端 | 极性只当通道；单窗拿不到运动方向。实测无标签 AUC 0.82–0.93，方向余弦 −0.98 | 独立；与速度假设/正则系配合更强 | 已实现 |
| 5 | 亮度变化重建（有符号泄漏累加，多 τ） | 前端 | 闪烁一个周期净和为零、目标是移动的暗斑；物理量而非计数 | 独立 | 设计 |
| 6 | 方差稳定（Anscombe 变换） | 前端 | 泊松计数方差随均值变化，网络要学方差 | 独立 | 候选 |
| 7 | 尺度空间计数谱 / 局部相关维数（log N 对 log r 的斜率） | 前端 | 只用计数同时得到局部维度与速度 | 独立 | 候选 |
| 8 | 主轴对齐的局部坐标（把邻域转到"沿轨迹/垂直轨迹"） | 前端/表示 | 同一目标因速度、方向不同而呈现不同模式 | **依赖 4 + 3** | 设计（方案二第二步） |
| 9 | 点过程节律统计（间隔均值、变异系数、Fano） | 前端 | 噪声 CV≈1，扫过的边缘不是；纯计数看不出 | 独立 | 候选 |
| 10 | 似然比表示 / 贝叶斯惊奇 | 前端 | "计数不是证据" | 已被判决层的 log r 取代 | 部分实现 |
| 11 | 共振器/递归谱（Goertzel）做闪烁陷波 | 前端 | 闪烁是线谱，目标是低频宽带 | 独立 | 候选 |
| 12 | 螺旋桨调制签名 | 前端 | 识别"是不是无人机" | — | **证伪**（EV-UAV 上周期图与泊松代理无差别；且文献已占） |
| 13 | 事件计数时钟 / 速度归一化时间 / 事件率自适应步长 | 时基 | 对事件率、速度的尺度不变性 | 独立 | 候选 |
| 14 | 图 / 点云 / 八叉树 / 隐式神经场表示 | 表示格式 | 保持原生稀疏、连续时间查询 | 独立但工程重 | 候选 |
| 15 | 同像素 ON→OFF 延迟配对（昆虫 ESTMD 原形） | 前端 | 运动方向 | — | **证伪**（目标 2.3% < 背景 3.5%） |

---

## S2 神经元与记忆

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 16 | **推导出来的泄漏**（补偿项 ψ = g，"预测落空 = 负证据"） | 判决神经元 | 固定 τ 与证据无关：强证据与弱证据按同样速率遗忘 | **依赖 2（μ0）+ 38（强度预测）** | 已实现 |
| 17 | **管道强度记忆** G_v = max(平移后的预测, ρ·平移后的旧 G) | 判决神经元第二隔室 | 管道离开目标后证据恒为 0、分数冻结并漂移（实测 92% 虚警离目标 >4 px） | **依赖 18 + 38** | 已实现 |
| 18 | **速度假设漂移膜电位**（drift LIF，沿轨迹积累） | 神经元/记忆 | 逐像素记忆不跟着目标走；V1 的跨窗状态可被替代（reset 只低 0.016） | 依赖 38 提供 r；退化为单假设即静止积累 | 已实现 |
| 19 | 快减慢带通神经元 / 自适应阈值 ALIF | 骨干神经元 | 膜电位极端化（V1 dec3 55–63%，V2 更差）、杂波归一化、τ 学不长 | 独立（V1 也能直接加） | 设计 |
| 20 | 可学突触延迟 + 符合检测（EMD / 结构张量） | 骨干神经元层 | 一阶状态表示不了位移与方向 | 独立 | 设计 |
| 21 | 共振 / 复特征值神经元（带通、周期抑制） | 骨干神经元 | 闪烁是周期性的，漏积分滤不掉 | 独立 | 候选 |
| 22 | 二阶时空矩神经元（约当块 SSM） | 神经元状态 | 一阶指数加权计数的表示上界 | 与 3 功能重叠（现放在前端，不在神经元内） | 部分实现 |
| 23 | 卡尔曼型不确定度状态（均值 + 方差） | 神经元 | 证据可信度没有表示 | 独立 | 候选 |
| 24 | 预测性辅助任务（用状态预测下一窗事件图） | 训练目标 | "记忆没用" | 已被 38+40 取代（预测 + 检验） | 部分实现 |
| 25 | 可并行训练的脉冲神经元（PSN / 线性递推） | 神经元与训练 | 长 BPTT 成本高 | 独立 | 候选 |

---

## S3 网络结构与计算组织

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 26 | 去掉空间下采样 + 膨胀卷积 | 骨干 | 目标 6×5 像素，下采样 3 次后在深层不可分（采样定理） | 独立 | 设计 |
| 27 | 时间金字塔（快 10 ms / 中 50 ms / 慢 250 ms） | 骨干 | 长时程上下文昂贵；空间金字塔对小目标有损、时间金字塔无损 | 独立 | 设计 |
| 28 | 分块稀疏执行 + 亚临界发放率正则（脉冲阈值 = 稀疏性控制器） | 执行 + 损失 | 代价由分辨率而非事件数决定（实测 16×16 块平均 33% 有事件，稠密序列 99%） | **28 与 51 必须一起** | 设计 |
| 29 | 因果 3D 分解卷积（空间核 × 时间延迟核） | 骨干 | 各向同性 2D 核表示不了有向时空滤波器 | 独立（与 20 二选一） | 候选 |
| 30 | 速度空间主干（在 (x, y, v) 上卷积） | 骨干 | 在速度维上直接做目标级推理 | **依赖 18** | 候选 |
| 31 | 伽利略等变提升 + 权重共享 | 骨干 | 每个速度都要单独学；训练数据不够 | **依赖 8**（正则坐标） | 设计（方案二） |
| 32 | DoG 侧抑制（固定的大小选择性前置滤波） | 前置层 | 闪烁与大面积结构 | 独立、零参数 | 候选 |
| 33 | 两阶段 where/what（低分辨率候选 + 高分辨率确认） | 调度 | 计算量 | 独立 | 候选 |
| 34 | 目标槽记忆 / 全局竞争 WTA | 目标层 | 网络里没有"目标"这个变量，而 Pd/Fa 是目标级指标 | 独立但工程重 | 候选 |
| 35 | 场景体制 MoE（按事件率、主速度调制） | 骨干调制 | 不同体制下最优统计量不同 | 独立 | 候选 |
| 36 | 有限滞后双向平滑（对最近 Δ 的缓冲跑一遍反向路径） | 骨干 | 因果性的代价（实测 Δ=0 → 对称，IoU 0.68 → 0.855） | 与 42 配合 | 候选 |
| 37 | 液体状态机（随机固定储备池）对照 | 对照实验 | 记忆的收益有多少来自训练 | 独立 | 候选 |

---

## S4 判决与读出

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 38 | **强度头**：输出可被下一窗检验的目标强度场 | 读出头 | 网络输出没有可被检验的量；判决层没有预测可用 | **与 40、47 强耦合** | 已实现 |
| 39 | **mark 头**：校准的逐事件 logit（无 pos_weight） | 读出头 | pos_weight 破坏校准，阈值 0.9 失去统计含义 | 独立 | 已实现 |
| 40 | **漂移 CUSUM 判决 + 位置级告警 + 虚警上界** | 判决层 | 阈值无统计含义、无虚警控制、何时报警无依据 | **依赖 2 + 38 + 41**；告警评估依赖 60 | 已实现 |
| 41 | **lme / mean 聚合（对空间相关稳健）** | 判决层 | 邻域求和要求像素条件独立，闪烁与成团杂波不满足（反例：sum 超界 1.2 倍） | 依赖 40 | 已实现 |
| 42 | **anytime 有界延迟读出 / 轨迹证据融合分数** | 读出 | 决策时刻固定在窗末；延迟曲线证明多等 250 ms 值 +0.045 | 融合依赖 40；"报曲线"这一形式独立 | 已实现 |
| 43 | 轨迹池化读出（时空连通分量共享证据） | 读出 | 逐像素判决之间没有一致性约束 | 独立（也能接在 V1 上） | 设计 |
| 44 | 几何条件化读出（事件相对主轴/重心的位置） | 读出 | 读出层不知道事件在轨迹上还是旁边 | **依赖 3 + 4** | 候选 |
| 45 | 首脉冲时间编码的"有无目标"预警 | 读出 | 超低延迟的粗判 | 独立 | 候选 |
| 46 | 检测与跟踪一体（管道状态即轨迹，实例关联直接读） | 读出/目标层 | 目标级输出与实例关联 | **依赖 18** | 候选 |

---

## S5 损失与训练

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 47 | **点过程似然损失**（强度场的泊松 NLL） | 损失 | 强度没有定义；BCE 学不出"率" | **与 38 耦合** | 已实现 |
| 48 | **去掉 pos_weight 的逐事件 BCE** | 损失 | 校准被破坏，模型间不可比 | 独立 | 已实现 |
| 49 | **验证集校准融合权重** | 后处理 | 融合分数不是严格后验，需要经验校准 | 依赖 42 | 已实现 |
| 50 | 因果版 STC 时空先验（邻域只取过去、按线状度加权） | 损失 | 原 STC 混淆 1 维与 2 维（闪烁 97% 虚警） | 独立 | 设计 |
| 51 | 发放率 / 亚临界正则 | 损失 | 稀疏性只被统计、没被训练 | **与 28 一起** | 设计 |
| 52 | 延迟感知损失（正确且早的判决权重更高） | 损失 | 训练目标与部署目标（等虚警率下最早检出）不一致 | 配合 42 | 候选 |
| 53 | 软标签（按到轨迹主轴的距离） | 标签 | 框派生标签的边缘噪声（实测仅 1–2%，优先级低） | 独立 | 候选 |
| 54 | 保标签的伽利略 / 旋转增强 | 训练数据 | 速度与方向的泛化；相机运动 | 独立 | 设计（方案二） |
| 55 | 偶极一致性 / 对比度最大化自监督 | 训练 | 速度与方向可以无标注学 | **依赖 3 + 4** | 设计 |
| 56 | **合成数据生成器 + 压力测试预设** | 数据 | 小数据、定理无法证伪；相关背景、背景突变、目标闪现 | 独立 | 已实现 |
| 57 | 弱监督 / EM（窗级标签或无标签） | 训练 | 标注效率、跨数据集 | **依赖 38 + 47** | 设计 |

---

## S6 评估协议

| # | 创新点 | 改哪里 | 解决什么 | 依赖 | 状态 |
|---|---|---|---|---|---|
| 58 | 延迟感知的逐事件协议（按实际发布时刻计延迟、报 Pd–延迟曲线） | 评估 | 离线 IoU 不含延迟；STARE 只覆盖跟踪 | 独立 | 部分实现 |
| 59 | 等虚警率比较（阈值扫描） | 评估 | 固定阈值让不同模型落在不同工作点 | 独立 | 已实现（V1 起） |
| 60 | 位置级告警指标（检出率、首次告警延迟、虚警连通域率 + 理论上界） | 评估 | 告警输出与逐事件 Fa 是不同的随机量 | 依赖 40 | 已实现 |
| 61 | 分场景报告（稀疏 / 闪烁 / 稠密） | 评估 | 全局 IoU 被两条稠密序列主导（占 59% 事件） | 独立 | 部分实现 |
| 62 | 精度—延迟—能耗帕累托 | 评估 | 三者需要一起看 | 独立 | 候选 |
| 63 | 廉价基线对照（BAF/EBF 去噪器、几何特征 + 树） | 评估 | 证明网络相对简单规则的必要性（树模型 test 0.855） | 独立 | 候选 |

---

## 必须成组使用的四个簇

**簇 A（核心，v2-1 已实现，缺一不可）**
```
2 背景率模型 ──► 38 强度头 + 47 点过程损失 ──► 40 漂移 CUSUM（含 16 推导泄漏、17 管道记忆、18 速度漂移、41 稳健聚合）
                                            └──► 42 延迟读出 / 60 告警评估
```
去掉 2，判决层没有背景补偿，虚警上界的前提不成立；去掉 38/47，判决层没有可检验的预测；去掉 41，空间相关背景下保证失效；去掉 17，分数会冻结漂移。

**簇 B（正则系 / 等变，方案二）**：4 偶极 + 3 矩 → 8 主轴对齐 → 31 等变提升 →（可选）30 速度空间主干、55 自监督、54 增强。
其中 3、4 已经作为特征单独可用；8 之后的部分必须依次叠加。

**簇 C（稀疏计算）**：51 发放率正则 + 28 分块稀疏执行，可选加 27 时间金字塔、26 去下采样。
只做 28 不做 51，活动会超临界、稀疏收益消失。

**簇 D（anytime）**：42 延迟读出 + 58 延迟感知评估，可选加 52 延迟感知损失、36 有限滞后平滑。

## 可以单独用的（插到 V1 或 V2 都行）

2 背景归一化、3 时间矩、4 偶极、5 亮度重建、19 带通/ALIF 神经元、20 延迟符合层、26 去下采样、27 时间金字塔、
32 DoG、39 无加权 BCE、43 轨迹池化、50 因果 STC、53 软标签、54 增强、56 合成数据、58–63 各项评估协议、63 廉价基线。

## 已否定，不再投入

15 同像素极性配对（数据否定）、12 螺旋桨签名（数据 + 文献双否定）、加长 TBPTT（V1 阶段已排除）。

---

## 建议的论文集合

**主线（已实现，一篇文章的骨架）**：簇 A 全部 + 3/4 前端特征 + 48 校准损失 + 56 合成验证 + 58/59/60/61 评估协议。
一句话：*网络给出可被下一窗检验的目标强度预测，命中与落空都沿运动轨迹积累成证据，何时报警由证据决定并有虚警上界。*

**第二梯队（若主线结果成立再加）**：19 带通神经元（治膜电位饱和）、26+27（结构）、簇 C（效率章节）、54 增强 + 簇 B 第一步（泛化）。

**留给下一篇**：簇 B 完整版（等变）、34 目标槽、57 弱监督、1 传感器闭环、14 隐式表示。
